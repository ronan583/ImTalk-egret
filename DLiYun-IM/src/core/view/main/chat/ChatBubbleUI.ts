module dliyun {
	export class ChatBubbleUI extends eui.Component{
		public constructor() {
			super();
		}

        public partAdded(partName: string, instance: any): void {
            super.partAdded(partName, instance);
        }		
	}
}